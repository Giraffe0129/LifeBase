# My Awesome App — 项目架构与部署指南

## 一、当前架构（本地开发模式）

```
你的电脑（Windows）
├── 前端 (Vite + Vue 3)    localhost:5173
├── 后端 (FastAPI + Uvicorn) localhost:8000
└── 数据库 (SQLite)          backend/app.db
```

- **前端**：Vue 3 + Vite 开发服务器，运行在 `localhost:5173`
- **后端**：FastAPI 应用，运行在 `localhost:8000`，处理所有 API 请求
- **数据库**：SQLite 文件 (`backend/app.db`)，存在你的电脑硬盘上
- **WebSocket**：后端提供实时推送，多个浏览器标签页之间自动同步

> 这种模式只适合你自己开发测试。电脑关机后服务器停止，所有人都无法使用。

---

## 二、离线同步机制（支持断网使用）

项目内置了离线支持，即使在无网络环境下也能操作：

```
在线时 ──→ API 请求直接发到服务器 ──→ 同时缓存到 IndexedDB
离线时 ──→ 操作写入 IndexedDB ──→ 加入 syncQueue 同步队列
恢复在线 ──→ 自动推送队列中的变更到服务器 ──→ 拉取全量数据刷新
```

- 手机上的 PWA（或 App）在连上过一次后，数据会缓存在本地
- 离线时创建/修改/删除的操作会进入同步队列
- 网络恢复后自动同步到服务器
- **但登录/注册必须在线**（需要服务器验证）

---

## 三、常用命令

```bash
# 启动后端（必须先 cd backend）
cd backend
uvicorn app.main:app --reload --port 8000

# 启动前端开发服务器（另一个终端）
cd frontend
npm run dev

# 构建前端生产版本
cd frontend
npm run build

# 构建移动端（Android）
cd frontend
npm run build
npx cap sync android
npx cap open android

# 构建桌面端（Electron）
cd frontend
npm run build:electron
```

---

## 四、部署到公网（让同学随时随地能用）

### 方案 A：部署到云服务器（推荐）

需要一台 **24 小时开机的云服务器**，最便宜的学生机/轻量云即可。

#### 步骤 1：准备后端

```bash
# 在云服务器上
git clone 你的项目
cd backend

# 安装依赖
pip install -r requirements.txt

# 修改 .env 配置
```

创建生产环境 `.env` 文件：

```env
# 改用 PostgreSQL（SQLite 并发差，只适合本地开发）
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/my_app

# 务必换一个随机密钥！
SECRET_KEY=这里填写一个随机的64位字符串

# 允许前端的域名
CORS_ORIGINS=https://你的域名.com

# 生产环境建议关掉 reload
```

#### 步骤 2：启动后端（用进程管理器保证不挂）

```bash
# 安装 uvicorn 的生产级运行方式
pip install gunicorn

# 或者用 systemd / supervisor 管理进程
# 简单的启动方式（不推荐，会随 SSH 退出而停止）：
uvicorn app.main:app --host 0.0.0.0 --port 8000

# 推荐：用 systemd 写成服务，开机自启
```

#### 步骤 3：构建前端并部署

```bash
cd frontend
npm run build

# 把 dist/ 目录放到 Nginx / Caddy 等 Web 服务器下
# Nginx 配置示例：
```

Nginx 配置：

```nginx
server {
    listen 80;
    server_name 你的域名.com;

    # 前端静态文件
    root /path/to/frontend/dist;
    index index.html;

    # 所有 API 请求转发到后端
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # WebSocket 转发
    location /ws {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # SPA 路由（所有非文件请求返回 index.html）
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

#### 步骤 4：手机端连接云服务器

修改 `frontend/src/api/index.ts` 中的 `BASE_URL`：

```ts
const BASE_URL = import.meta.env.PROD ? 'https://你的域名.com' : ''
```

然后重新构建移动端：

```bash
cd frontend
npm run build
npx cap sync android
npx cap open android   # 重新打包 APK
```

---

### 方案 B：局域网访问（临时方案，电脑不能关机）

如果你想在**同一 WiFi 下**让手机或同学的电脑使用：

#### 1. 找到你电脑的局域网 IP

```cmd
ipconfig
# 找到类似: IPv4 地址 . . . . . . . : 192.168.x.x
```

#### 2. 修改后端 CORS 配置

编辑 `backend/.env`，把 CORS 加上你的局域网 IP：

```env
CORS_ORIGINS=http://192.168.x.x:5173,http://localhost:5173,http://localhost:5174
```

#### 3. 修改前端 API 地址

编辑 `frontend/src/api/index.ts`：

```ts
// 改成你电脑的局域网 IP
const BASE_URL = 'http://192.168.x.x:8000'
```

#### 4. 启动后端（监听所有网卡）

```bash
cd backend
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

#### 5. 启动前端

```bash
cd frontend
npm run dev -- --host 0.0.0.0
```

#### 6. 手机/同学访问

- **浏览器**：访问 `http://192.168.x.x:5173`
- **手机 App**：如果是用 Capacitor 打包的 App，需要重新打包并指向局域网地址
- **注意**：你的电脑必须开机、不能休眠、不能切换 WiFi

---

## 五、服务器架构总结

| 场景 | 服务器在哪 | 数据存哪 | 能否使用 |
|------|-----------|---------|---------|
| 现在（本地开发） | 你的电脑 | 你的电脑 `app.db` | 你的电脑开机才能用 |
| 局域网访问 | 你的电脑 | 你的电脑 | 电脑开机+同一 WiFi |
| 部署到云服务器 | 云服务器 | 云服务器数据库 | 24 小时可用 |
| 发给同学 APK（本地模式） | 你的电脑 | 你的电脑 | 电脑开机才能用 |
| 发给同学 APK（云部署后） | 云服务器 | 云服务器 | 24 小时可用 |

---

## 六、数据安全与多用户

- **多用户隔离**：每个用户注册后有自己的账号，数据按 `user_id` 隔离
- **JWT 认证**：登录后拿到 Token，后续请求通过 Token 识别身份
- **密码加密**：使用 bcrypt 哈希存储，不存明文
- **如果部署到云服务器**：所有用户的数据都会存在云服务器的数据库里，互相不可见

---

## 七、推荐部署流程（给同学用）

```
1. 购买云服务器（腾讯云轻量、阿里云ECS、Railway 等都行，最低配够用）
2. 安装 Python、Node.js、PostgreSQL
3. 把项目代码传到服务器
4. 安装后端依赖，配置环境变量
5. 启动后端（用 systemd 做开机自启）
6. 构建前端，用 Nginx 部署
7. 打包移动端 APK，指向云服务器地址
8. 把 APK 发给同学安装
```

这样你的同学无论在哪里，只要有网络就能打开用，你也不用一直开着电脑。
