# 移动端 (Capacitor/Android) 后端服务器地址
# 部署 APK 到手机前，请将此处改为你电脑当前的局域网 IP
# 查看 IP 方法：Windows 打开 cmd 输入 ipconfig，找 IPv4 地址
# 然后执行: npm run build && npx cap sync android
# 最后用 Android Studio 构建 APK
VITE_API_URL=http://192.168.x.x:8000
